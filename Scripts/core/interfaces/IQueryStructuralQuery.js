function IQueryStructuralQuery(){}IQueryStructuralQuery.Interface("IQueryStructuralQuery");IQueryStructuralQuery.prototype.AgreeQueryRouteChange=function(oldrouteType,newrouteType,router){return true;}.Description("Notifies the query about routing type change. The query must return true if it agrees with that and false otherwise. Disagreeing with the change will stop the propagation.").Param("oldrouteType","String - the old routing type (e.g. dom, window, app)").Param("newrouteType","String - the new routing type (e.g. dom, window, app)").Param("router","(recommended, but optional) A reference to the object that performs the routing (it is the caller's object, actually). ").Returns("true for agreement, false for disagreement");IQueryStructuralQuery.prototype.AgreeQueryProcessing=function(processor,processInstructions){return true;}.Description("Notifies the query that it is about to be processed. The query can agree (true) or disagree (false) with that. The disagreement produces the equivalent effect as if a processor has not been found.").Param("processor","A reference to the processor object").Param("processInstructions","The process instructions whith which the query has been thrown.").Returns("true for agreement and false for disagreement");