function SystemFormatterBase(){CustomFormatterBase.apply(this,arguments);}SystemFormatterBase.Inherit(CustomFormatterBase,"SystemFormatterBase");SystemFormatterBase.Implement(IFormatterRegistrationImpl);SystemFormatterBase.prototype.$ToTarget=function(host,val,bind,args){var arr=Array.createCopyOf(arguments,1,3);var farg=this.$parseArguments(args);arr.push(farg);return this.Read.apply(this,arr);};SystemFormatterBase.prototype.$FromTarget=function(host,val,bind,args){var arr=Array.createCopyOf(arguments,1,3);var farg=this.$parseArguments(args);arr.push(farg);return this.Write.apply(this,arr);};SystemFormatterBase.prototype.Read=function(val,bind,params){throw"Read not implemented in formatter "+this.fullClassType();}.Description("Implements conversion while reading to target").Param("val","The raw source value").Param("bind","The reference to the binding in which this is defined. If it isn't called through binding this argument will be null").Param("params","Depending on the argument list parser, without one it is string or null. For other choices implement IArgumentListParserStdImpl").Returns("The converted/collected/formatted value");SystemFormatterBase.prototype.Write=function(val,bind,params){throw"Write not implemented in formatter "+this.fullClassType();}.Description("Implements conversion while writting to source").Param("val","The raw value read from the target or passed directly (if not used in binding)").Param("bind","The reference to the binding in which this is defined. If it isn't called through binding this argument will be null").Param("params","Depending on the argument list parser, without one it is string or null. For other choices implement IArgumentListParserStdImpl").Returns("The converted/collected/parsed value");