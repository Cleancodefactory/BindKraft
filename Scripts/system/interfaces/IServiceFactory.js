function IServiceFactory(){}IServiceFactory.Interface("IServiceFactory","IRequestInterface");IServiceFactory.prototype.GetService=function(alias,iservice,std_instance_parameters){throw"Not implemented";}.Param("std_instance_parameters","Standard parameters in textual or object form - the same as the data-class parameters").Param("alias","A service can be more than a single interface or can be provided through different interfaces - so we need alias and interface to unique identify a service").Returns("Operation");