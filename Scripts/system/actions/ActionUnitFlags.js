var ActionUnitFlags = {
	None: 			0x0000,
	Default: 		0x0001, // Used remembered defaults (the system) if available.
	NoDefault: 		0x0002 // Incompatible with Default. Make selection anew ignoring any remembered defaults (the system)
};