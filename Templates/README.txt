The templates for the BindKraft module are a "special case". These must be as simple as possible
with none or minimal CSS - enough to illustrate the connection between the component and the templates
so that styled templates can be created in separate modules.

this directory was empty in the initial check-in, because we did not maintain such a set before and we
need to extract it from templates in use.